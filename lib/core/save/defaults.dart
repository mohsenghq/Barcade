/// New-player seed state (recovery ladder step 3, and first install).
library;

import '../model/chess_profile.dart';

/// A fresh profile: Nebula theme, letter pieces, no counters.
ChessProfile newChessProfile() => const ChessProfile();
