# Node.js
node_modules/
*.tsbuildinfo
.next/
.vercel/

# Dart/Flutter
.dart_tool/
.packages
build/
.flutter-plugins*
*.iml
.idea/

# OS
.DS_Store

# Local tooling
tool/.env.local

# Trained model artifact — fetched via tool/fetch_net.dart.
# CI fetches before every build so the model is always embedded.
# Locally, run `dart run tool/fetch_net.dart` before `flutter build`.
assets/ai/chess_net.onnx
assets/ai/manifest.json
assets/ai/.gitkeep

# Web app model (copied from assets/ai/ by tool/fetch_web_model.mjs)
apps/web/public/chess_net.onnx

# Training repo (cloned separately)
rl-for-games/
